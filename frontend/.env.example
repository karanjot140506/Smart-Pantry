# Base URL the frontend proxies /api requests to during local dev (see vite.config.js).
# Not needed in production — the backend serves the built frontend directly.
VITE_API_BASE_URL=http://localhost:8080
